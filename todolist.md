## 1.唤醒方式

## 2.asr时延不稳定原因检查，也需要检查时延统计

## 3.用户画像的消融激活（删除这一层用户画像，验证用户画像是否生效）
1. 设计测试用例 \
围绕用户画像中的不同层级设计多组对话输入，确保每组输入能够自然触发某一类画像信息。测试用例应覆盖核心需求、情绪调节方式、认知表达风格、身份背景和行为偏好等维度。

2. 设置完整画像基线 \
在保留完整用户画像的情况下运行算法，记录智能体回复内容、被激活的用户画像字段、回复风格和个性化程度，作为对照组结果。

3. 逐层删除用户画像 \
分别删除用户画像中的某一层，例如 core、regulation、cognition、identity、behavior，每次只删除一层，其余画像保持不变。

4. 重复运行相同测试用例 \
对每个消融版本使用与基线完全相同的用户输入，记录智能体回复和用户画像激活日志，确保差异来自画像层级删除，而不是输入变化。

5. 对比激活结果 \
比较完整画像和消融画像下的激活字段变化，观察被删除层级是否不再被激活，以及相关层级缺失后是否影响其它画像字段的调用。

6. 对比回复差异 \
分析智能体回复在内容、语气、具体性、关系感和个性化程度上的变化，判断删除某层画像后是否导致回复变得更泛化或不贴合用户。

7. 评估画像有效性 \
如果删除某层画像后，相关测试用例中的激活字段减少，且回复质量或个性化程度下降，说明该层画像对生成回复具有实际作用。若删除后回复几乎没有变化，则说明该层画像在当前任务中贡献较弱，或测试用例没有充分触发该层画像。

8. 汇总消融结论 \
统计不同画像层级的激活频率、回复变化程度和典型案例，形成表格或分析报告，用于说明哪些用户画像层级最影响智能体回复。

